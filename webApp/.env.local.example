# Backend base URL. Used by vite dev proxy and Vercel edge proxy function.
# No trailing slash. Must be reachable from your dev machine (and from Vercel in prod).
API_BASE=https://your-backend.example.ts.net
